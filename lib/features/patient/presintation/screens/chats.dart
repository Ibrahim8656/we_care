import 'package:flutter/material.dart';

class chatsScreen extends StatelessWidget {
  const chatsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("chatsScreen Screen")),
    );
  }
}